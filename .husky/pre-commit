[[ -z "${CI}" ]] || exit 0

SCRIPT_DIR=$( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )
REPO_DIR=${SCRIPT_DIR%/*}

[[ `git diff --staged --name-only` =~ content.* ]] && ${REPO_DIR}/scripts/run_checks.sh || exit 0
